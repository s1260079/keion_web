<div  class="allPage">
  <p id="notice"><%= notice %></p>
  <h1 >Members</h1>
  <table class="table" style="margin-top: 80px">
    <thead>
      <tr>
        <th>Student Id</th>
        <th>Name</th>
        <th>Part</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.student_id, user_path(user) %></td>
          <td><%= user.user_name %></td>
          <td><%= user.part %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>
    <%= link_to 'New User', new_user_path %>
</div>
